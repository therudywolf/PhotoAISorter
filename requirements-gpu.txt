# PyTorch with CUDA 12.6 (e.g. RTX 4070). Install AFTER base requirements:
#   .venv\Scripts\pip install -r requirements-gpu.txt --force-reinstall
# --force-reinstall is required: a CPU "torch==X" already satisfies "torch>=Y",
# so a plain `pip install --upgrade` is a no-op and never fetches the CUDA wheel.
# cu126 carries current torch (2.12.x); the older cu124 index caps at torch 2.6.
--index-url https://download.pytorch.org/whl/cu126
torch>=2.4.0
torchvision>=0.19.0
