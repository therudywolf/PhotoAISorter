# PyTorch with CUDA 12.4 (e.g. RTX 4070). Install AFTER base requirements:
#   .venv\Scripts\pip install -r requirements-gpu.txt
--index-url https://download.pytorch.org/whl/cu124
torch>=2.4.0
torchvision>=0.19.0
